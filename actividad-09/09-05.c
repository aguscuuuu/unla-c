/*
    Escribir una función para agregar un estudiante que sólo reciba los datos del mismo, lo cree y lo agregue
    en la primera posición libre:
    int agregarEstudiante(CursoPtr curso, char* nombre, char* apellido, int legajo, int dni);
    La función devolverá el índice en el que se agregó el estudiante o -1 si no hay lugar para agregar en
    el array.
    --------------------------------------------------------------------------------------------------------
*/
#include <stdio.h>
#include <stdlib.h>

int main(){



    return 0;
}
