/*
    Escribir una función para eliminar un estudiante del array de estudiantes:
    int eliminarEstudiante(CursoPtr curso, int indice);
    La función debe verificar que indice no sobrepase los límites del array y que haya un estudiante en la
    misma (que sea distinto a NULL). Una vez eliminado el estudiante, se deben mover los estudiantes siguientes
    hacia arriba, es decir, no deben quedar huecos en el array. La función devolverá el índice del elemento
    eliminado o -1 si no puedo eliminarlo (porque el índice superó los límites o no había un estudiante en
    la posición).
    --------------------------------------------------------------------------------------------------------
*/
#include <stdio.h>
#include <stdlib.h>

int main(){



    return 0;
}
