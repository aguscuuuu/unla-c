/*
    Escribir una función que reciba los datos de un estudiante y un índice y lo cree en memoria dinámica para
    luego agregarlo al array de punteros a estudiante:
    int agregarEstudiantePorIndice(CursoPtr curso, char* nombre, char* apellido, int legajo, int dni, int indice);
    La función debe controlar que el índice no sea mayor que el que admite el array de estudiantes y que la
    posición en la que queremos agregar contenga NULL. Debe devolver el índice si lo pudo agregar y -1 en caso
    contrario.
    --------------------------------------------------------------------------------------------------------
*/
#include <stdio.h>
#include <stdlib.h>

int main(){



    return 0;
}
