/*
    Completar el modelo anterior con las funciones de creación y destrucción de Docente y Curso en memoria
    dinámica. Utilizar memoria dinámica para los campos tipo char*.

    DocentePtr crearDocente(char* nombre, char* apellido, int legajo, int cuil);
    DocentePtr destruirDocente(DocentePtr dp);

    CursoPtr crearCurso(char* nombre, DocentePtr docente, int anio, int cuatrimestre);
    CursoPtr destruirCurso(CursoPtr cp);

    La función crearCurso debe inicializar el array de punteros a estudiantes con NULL en todas sus
    posiciones. destruirCurso se debe encargar de liberar toda la memoria dinámica que se haya tomado para
    los distintos componentes de Curso, incluyendo los estudiantes.
    --------------------------------------------------------------------------------------------------------
*/
#include <stdio.h>
#include <stdlib.h>

int main(){



    return 0;
}
